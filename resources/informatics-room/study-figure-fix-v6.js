/* 情報Ⅰ 図解の意味関係を校正する v7
   「順序があるもの」だけに矢印を使い、分類・並列概念を誤って流れとして見せない。
   原教材の側注まで再照合し、必要な概念は図にも反映する。 */
(() => {
  const f=window.ELECTRONIC_FIGURES_V4||{};
  if(f['b2-1']) f['b2-1']={
    type:'axes',title:'コミュニケーションを2つの軸で分類',
    items:[['時間の軸','同期（電話・対面など） ↔ 非同期（電子メールなど）'],['人数の軸','1対1（特定相手） ↔ 1対多（放送など）']],
    note:'一つのメディアを「時間」と「人数」の別々の軸で分類する。チャットのように使い方で同期性が変わるものもある。'
  };
  if(f['b4-1']) f['b4-1']={
    type:'axes',title:'情報デザインを二段階で整理',
    items:[['目的の違い','デザイン：目的・利用者・問題解決を意識 ／ アート：芸術表現・問題提起を重視'],['表現の3手法','抽象化＝細部を省く ／ 可視化＝見える形へ ／ 構造化＝関係・順序を整理']],
    note:'「伝える目的」を先に定め、その目的に合わせて抽象化・可視化・構造化を選ぶ。'
  };
  if(f['b4-2']) f['b4-2'].type='table';
  if(f['b5-1']) f['b5-1'].type='hub';
  if(f['b5-3']) f['b5-3']={
    type:'table',title:'論理回路は真理値で読む',
    items:[['AND','両方1 → 1'],['OR','少なくとも一方1 → 1'],['NOT','0↔1を反転'],['XOR','一方だけ1 → 1'],['NOR','ORの出力をNOT'],['NAND','ANDの出力をNOT']],
    note:'複合回路は、各ゲートの中間出力を真理値表で追う。半加算器では和にXOR、桁上がりにANDの考え方を使う。'
  };
  if(f['b6-5']) f['b6-5'].type='table';
  if(f['b6-6']) f['b6-6'].type='table';
  if(f['b6-7']) f['b6-7'].type='table';
  if(f['b7-1']) f['b7-1'].type='axes';
  if(f['b8-2']) f['b8-2']={
    type:'layers',title:'TCP/IP 4階層と通信の役割',
    items:[['アプリケーション層','HTTP・SMTP・POP/IMAPなど：利用目的に近い通信'],['トランスポート層','TCP / UDP：届き方・信頼性・速度'],['インターネット層','IP：宛先IPアドレスと経路'],['ネットワークインタフェース層','Ethernet・無線LAN・PPP：実際の伝送']],
    note:'送信側では上→下、受信側では下→上に対応する処理を行う。OSI 7階層は別の階層モデルで、TCP/IP 4階層と名称を1対1で暗記しない。'
  };
  if(f['b9-4']) f['b9-4']={
    type:'axes',title:'データの解釈2は3つの話を分けて整理',
    items:[['回帰','独立変数X → 回帰式 → 従属変数Yの予測。残差と最小二乗法を理解する。'],['標本から母集団へ','母集団 → 無作為抽出した標本 → 推定。標本誤差を意識する。'],['仮説検定','帰無仮説と対立仮説を置き、p値と有意水準から棄却できるか判断する。']],
    note:'回帰・推定・仮説検定は同じ一本道ではない。目的の異なる3ブロックを区別し、資料問題の中で必要なものを使う。'
  };
})();